supabase==2.10.0
python-dotenv==1.0.1
click==8.1.7
httpx>=0.27.0
# MCP サーバ (Model Context Protocol). 決定論ロジック層をツール公開する。
mcp>=1.2.0
# AWS Bedrock (Titan Text Embeddings V2) — doc_chunks のベクトル検索 (ADR-017)
boto3>=1.34.0
# Gemini 利用時のみ必要 (LLM_PROVIDER=gemini の場合)
# google-generativeai>=0.8.0
