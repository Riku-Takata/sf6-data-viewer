# Lambda (MCP サーバ) パッケージング用の依存マニフェスト。
# SAM build が CodeUri=src/ 配下のこのファイルを見て zip にインストールする。
# ローカル開発用の依存はリポジトリ root の requirements.txt を参照。
mcp>=1.2.0
mangum>=0.17.0
supabase==2.10.0
boto3>=1.34.0
python-dotenv==1.0.1
httpx>=0.27.0
